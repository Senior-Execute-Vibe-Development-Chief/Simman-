{
  "dev": {
    "substrateMilliseconds": 2300,
    "routingInitializeMilliseconds": 80,
    "customizeMilliseconds": 45,
    "queryMilliseconds": 95,
    "distanceMapMilliseconds": 75,
    "tickMilliseconds": 15
  },
  "target": {
    "substrateMilliseconds": 68000,
    "routingInitializeMilliseconds": 2800,
    "customizeMilliseconds": 2600,
    "queryMilliseconds": 3600,
    "distanceMapMilliseconds": 3800,
    "tickMilliseconds": 650
  },
  "notes": "M2 baseline (seed 42042, earth_sim, target 1800x900) measures the real people pass: 10 monthly ticks averaged ~11 ms at dev and ~486 ms at target on this runner. Values carry headroom for runner variance; CI fails above 120% of these. The target-grid people loop is the known performance finding in QUESTIONS.md and remains behind the same typed-array API for the later kernel drop-in."
}

{
  "dev": {
    "substrateMilliseconds": 1200,
    "routingInitializeMilliseconds": 12,
    "customizeMilliseconds": 25,
    "queryMilliseconds": 35,
    "distanceMapMilliseconds": 25,
    "tickMilliseconds": 4
  },
  "target": {
    "substrateMilliseconds": 30000,
    "routingInitializeMilliseconds": 150,
    "customizeMilliseconds": 500,
    "queryMilliseconds": 1400,
    "distanceMapMilliseconds": 1450,
    "tickMilliseconds": 150
  },
  "notes": "W2 baseline, seed 42042, earth_sim. On this runner the wasm people kernel measures 2.61 ms/tick at dev and 122.06 ms/tick at the shipped target over 10 ticks; target is a 3.95-hour YD-to-1 CE projection (116,412 monthly ticks). Baselines include runner headroom and the 20% ratchet compares each phase independently. The optional BENCH_LONG=1 arm records the target 1000-year wall-clock."
}
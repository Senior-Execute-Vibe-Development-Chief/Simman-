# Peer seats — the genesis lane fills the cradle (2026-08-20)

Owner, after reading the live realm census (window.nations, build d64a7ba):
**"there are not nearly ENOUGH nations. also the first nations are VASTLY too
large off the bat."** Ordered adversarial: this wave is the measurement that
found one missing channel under both charges, and the channel built.

## ERRATUM (2026-08-20, same day): probe grid labels

The harness halves `W` (W=480 → tw=240; W=960 → tw=480; the shipped app is
W=1920 → tw=960). The probe_catchment columns below therefore measured
tw=240 and tw=480 — one grid coarser than first labeled. The before/after
comparison is internally consistent (both arms ran the same real grid), and
the probe_shape results — the first-state birth size, the 387-realm
register, the size distribution — ran W=1920 and ARE the true shipped grid.
A true-grid (W=1920) catchment arm is recorded below the tables.

## The prosecution's numbers (probe_catchment, live arm, seed 8817)

| quantity | tw=240 / 26k | tw=480 / 27k | history at the same moment |
|---|---|---|---|
| market cells (the urban slot lattice) | 207 · median 636k km² | 205 · median 588k km² | — (resolution-invariant ✓) |
| cities seated | 71 | 66 | hundreds of 10k+ cores |
| **peer slots the peopled cells could feed** | **717** | **1,225** | — |
| median city catchment | 1.2–3.2M km² | 0.8–1.5M km² | city-state domain 1–5k km² |
| max catchment | 6.2M km² | 7.1M km² (36.5M people) | — |
| max catchment radius | ~3,000 km | ~2,563 km | von Thünen grain-shed ~30–50 km |
| `siteCity tallyBar` refusals | 17,949 (64 sites mint-ready, waiting) | 18,155 (49 waiting) | — |
| `fission noRivalSeat` | — | 2,039 | states died constantly |

The causal chain, measured at every link: the hydro-anchor site lattice holds
~206 cells planet-wide → the genesis lane (`maybeSiteCities`) hard-blocks any
claimed cell (`claims.claimed[k]`), and every other land channel proposes
candidates only AT cell anchors — where the incumbent already stands, so
`labelBasinFree`'s peer-capacity law (variance arc wave 1) is spacing-blocked
at distance zero, always. One court per ~600k km² cell, forever. Each court's
persistent territory then absorbs its whole basin (catchments of millions of
km²), each state inherits a subcontinental demographic base at declaration
(the first census row showed one member city holding 13.6M people before a
single tile was painted), fission finds no rival seat to receive a breakaway,
and the register tops out at dozens where history holds hundreds.

This is the variance arc's own "NOBODY KNOCKS" verdict
(docs/variance-arc-2026-08-13.md), whose named unlock — *"the site-city pass
must propose a SECOND SEAT in cells whose capacity exceeds their count — the
best popField peak ≥ 2×urbanCoreR from every seated label, through the same
eligibility gates"* — was specified, never built. The tribal peer lattice
shipped and delivered the 321-polity map then; the LAND_KNOW tallies bar
(2026-08-20, owner-directed empty prehistory) re-times that lane, and the
CITY peer lane never existed.

## What shipped (T.PEER_SEATS, def 1; harness pins 0)

`maybePeerSeats` in crystallize.js — the candidate generation the peerlat
funnel demanded, exactly as specified:

* A claimed cell with spare capacity (`count < floor(mass/basinBar)` —
  labelBasinFree's own law, zero new constants) grows a **peer gathering
  site**: its best standing popField peak that keeps every core whole
  (≥ 2×urbanCoreR from each seated label). Held positions are kept while
  legal (a pile mid-gather is never stranded); invalidated ones re-pick on
  the claims cadence.
* The peer site gathers by the **same drift law** (cell basin pays, core
  receives, paced by capacity headroom), is held by the **same spike law**
  (per-tick re-stamp; the anchor lane's birth-crater lesson), and mints
  through the **same tail** — `mintCityAt`, extracted verbatim from the
  anchor loop so both lanes birth cities identically (census cap, field
  debit, knowledge birthright, culture, the STATE_OF_LAND adoption door,
  basin granaries, spike handoff). Hashbase anchors prove the extraction
  byte-identical (fd90feea / 7239c843).
* The tally proof is the **cell's**: its ledger earned tallies when the
  first court seated (the mint required org ≥ the bar at freeze), or a
  living court in the cell carries organization past the bar itself (a
  colony can claim a cell whose ledger froze young). A fresh per-tile
  ledger would re-run prehistory under an already-literate incumbent.
* Downstream sovereignty is untouched and emergent: a peer born beyond any
  realm's paint self-anchors or materialises its land nation (new realms);
  one born on a realm's authored ground is born stateless into the
  adoption door — the cradle-monopoly question stays the next lap, now
  with actual peers standing in the cradle.

## After (same probes, same seeds, PEER_SEATS=1)

| quantity | tw=240 / 26k | tw=480 / 27k |
|---|---|---|
| cities | 71 → **574** | 66 → **879** |
| realms (register) | 62 → **437** | 50 → **610** |
| median city catchment | 1.2M → sub-tile (young peers) | 834k → **44k km²** |
| max catchment census | 10.2M (20% of humanity at peak) | 36.5M → **8.0M (~3%)** |
| top cradle cell | 1/48 | 1/74 → **33/63, 53/53, 48/48** |
| peerSeat funnel | — | knock→seat 1338; then `cellFull`/`noPosition` (capacity and the 2×coreR packing bound both bind — the physics limits, not the lattice) |

### The true-grid arm (W=1920 → tw=960 — the shipped app), landed after the erratum

| step | cities | realms | median catchment | max catchment | note |
|---|---|---|---|---|---|
| 16k | 26 | 0 | 90k km² | 1.68M | first cohort; cradle cell 14/31 |
| 22k | 182 | 113 | 77k km² | 2.67M | |
| 27k | **609** | **434** | **61k km²** | 1.58M | peerSeat: 630 seated; `noPosition=1792` — the 2×coreR packing bound binds FIRST at this grid (correct physics: cores exhaust legal positions before capacity) |

Median catchment at the shipped grid lands at **61k km²** — the historical
satrapy/province band — and the top of the catchment table is 0.07–0.94M km²
holding 1.4–9.6M people (the Nile lineage seat: 71k km², 1.9M). The
mega-catchment is dead at every grid.

Two true-grid funnel facts that sharpen the follow-ups: **integrate PASSED=0
of 4,290** (every vassal stays a vassal — orgBelowMin 2,613 + seatAboveTierCap
1,562), and **storm PASSED=7 of 11,597 / capture 10 of 2,914** while attack
PASSED=27,549 — the register's death-and-absorption half is essentially
inert at the shipped grid. The realm count can only ratchet upward until the
consolidation lap lands; the owner's step-38k app frames (989→1,038 realms,
Renaissance) are this ratchet observed live.

And the owner-facing size distribution (probe_shape, tw=960, live arm):

* **The first state is born at 8k km²** — was 310–878k. Three orders of
  magnitude, the exact charge ("vastly too large off the bat").
* step 24k: 169 realms, median 16k km², 91% under 100k km², Gini 0.68.
* step 27k: **387 realms (219 vassal), median 19k km², 92% small** (the
  1500CE-Europe band is ~80%), top-1 share 3%, and the flows finally churn
  like history: **founded 79 · ended 125 · seceded 89 · shattered 7** in the
  final interval — the old register had ZERO state deaths in 4,000 steps.
* The resolution inversion died with it: the finer grid now carries MORE
  cities and realms than the coarser (879/610 at tw=480 vs 574/437 at
  tw=240), and probe_shape confirms the register at the true shipped grid
  (387 territorial realms at tw=960/27k).

## Verdict and follow-ups

Both charges answered by one channel, no constants tuned. What the
measurement leaves OPEN, named:

1. **The imperial head is now missing** (max realm 438k km²; lnσ 1.31 vs
   real maps' 2.0–2.6). Right failure to have: empires must now be BUILT
   from the dense fabric by consolidation — and the war funnel shows the
   raw material fires (attack PASSED 31,660) while decision doesn't (storm
   `assaultTooWeak` ~24k, capture ~0). The variance arc's standing
   consolidation question — momentum, cascade, siege at city-state scale —
   is the next lap.
2. **The enclave-city transient**: a peer born on a realm's painted ground
   holds ~zero catchment until adopted (CATCHMENT_CLIP is doing its job on
   ground that is all claimed). Watch that adoption/materialisation keeps
   pace; if enclave cities pool permanently, the adoption economics
   (cradle-monopoly lap) is where the fix lives.
3. The mega-catchment top tail (a few 2–5M km² frontier catchments where no
   peer has yet seated) shrinks as cells fill; if it persists, the
   settlement reach budget still wants von-Thünen grounding — measure
   before building.
4. `freshCountryId` empty-bloc crash fixed en passant (a member city with no
   painted tiles — routine once young peers join realms — made
   `declareIndependence` filter to an empty bloc; the degenerate path now
   returns -1, the caller's existing vent).

Gates: hashbase anchors byte-identical (fd90feea / 7239c843 — extraction +
guard + pinned lever all invisible to the reference regime); smoke, validate,
resgate, coverage, lint green. PEER_SEATS joins DAWN_LIVE / STATE_RECORDS /
LAND_KNOW in the harness pin set; **the full live arm is now
`SIM_TUNE="DAWN_LIVE=1,STATE_RECORDS=1,LAND_KNOW=1,PEER_SEATS=1"`** and every
genesis-regime verdict must run it explicitly.

# Handoff — end of 2026-08-24 (the Egypt/density/food-economy day)

Read this first, then `docs/egypt-autopsy-2026-08-24.md` (the day's full
chronicle: every finding, verdict, erratum and run log reference lives
there). Branch `claude/nationless-cities-issue-7garql`, everything committed
and pushed. Tree state: SAVE_VERSION **45**; default hash baseline (320-grid
probe_hashbase, 31337/2500) is **c9562488** — the pre-v45 pairs in the older
probe headers are stale.

## What SHIPPED today (defaults ON, v45, full gates green)

- **T.ONE_BOOK** — ONE food book: the market ledger bills the MARKET-FED
  core (min(people, _urbanPop)), not the self-feeding ONE_POP countryside.
  Killed the planet-wide pseudo-famine (production:demand read 1/6–1/8
  forever), empty granaries, max-clamped prices, zero tradeable surplus.
- **T.SHIP_SURPLUS** — the farm gate sells its SURPLUS (pool − own need),
  retiring the village-world tier fractions. World grain flow ×6.
- Battery: stylized 3/3 seeds all hard gates (warnings 0/0/1, budget 2),
  resgate all bands, smoke green. Pre-v45 saves keep two-book physics
  (v<45 guard).

## Levers BUILT but OFF (verdicts recorded in their tuning descs)

- **T.LEAN_YEAR (def 0) — THE ONE THAT MATTERS.** Founding bars ×
  1/FAMINE_SEVERITY (≈2.9): a city only where its basin survives the famine
  year; dissolve stays 1× (stability band). **Live-regime referendum was
  the day's decisive win** (v45 control for attribution): register turnover
  375→93, big-anchor deaths 16→7, realms-through-Egypt 86→20, standing
  register 52→14 (bronze band), ONE realm holding Egypt unbroken
  21.5k→25k at 2.4–2.6× margins. **Blocked from default by ONE hard gate**:
  seed 777 in the seeded-mature harness (civilization alive: 10 settlements)
  — the flat planetary 2.9× strangles marginal-geography worlds. 4242's
  3-soft-warning excess is stability-read-as-anomaly by churn-era bands
  (616 living vs 24 fallen states). Safe and recommended in live play NOW:
  `LEAN_YEAR=1` in SIM_TUNE.
- T.CITY_HOLD / T.CATCH_GRACE (def 0) — the seat-death political fixes;
  measured RED standalone (executioner rotates; pinned cores can't shed →
  dissolution doubles). Re-trial AFTER density is fixed — the siege-warfare
  channel they enable is right physics.
- T.MINT_RESIDUAL / T.MINT_REACH (def 0) — mint-side bars; measured
  inert/red with the day's key structural lesson: **any local-availability
  bar is a saturation detector — it cannot cut BELOW saturation**, and the
  register equilibrates at pie/bar regardless.

## THE CHAIN for next session (each step unblocks the next)

1. **The moisture-calibration lap** ← START HERE. The recorded debt
   (Britain ≈ Mesopotamia on the moisture index; solver-Sahel cropland too
   wet; monsoon Ganges reads semi-arid) now mis-feeds TWO systems (forest
   signal then, yield-variance map now) and is the wave's gate. Fix the
   index (or add a corrective calibration layer), re-run
   `tools/probe_yieldcv.mjs` — target: the 8 missing regions land in band.
   Secondary input debts recorded in the autopsy doc's final section:
   great-river banding for the variance map's water term (use IRR_BAND
   in-mechanism; the tw=240 Tigris–Euphrates doesn't register), dryFrac's
   monsoon semantics (N. China / S. India read season 0.00).
2. **The harvest-years mechanism** (owner-ratified design): an annual
   regional harvest index — spatially correlated, year-persistent,
   composing with climate.js's century layer — multiplying landFood.
   Famine DERIVES from the tail (log famine.struck when a region goes below
   its p10); FAMINE_CHANCE/SEVERITY/RADIUS and famineVuln aiming retire;
   granaries do real work automatically. New persisted state ⇒ SAVE bump +
   coverage/monotone.
3. **LEAN_YEAR per-basin**: margin = each basin's own lean-year statistic
   from the variance map (Sahel huge → few cities; stable temperate modest
   → **seed 777 releases by physics**; Egypt ~2× from flood variance).
4. **LEAN_YEAR flip** (SAVE_VERSION 46) — re-run the exact ladder in the
   autopsy doc; also revisit the churn-era stylized soft bands (lifespans/
   war-rate) which now flag stability itself, per the ratchet rule.
5. Then: urban hierarchy/primacy (the agglomeration engine finally has
   margins + surplus + flowing imports to work with — primacy still ~1.1),
   CITY_HOLD re-trial, and the owner's tw=960 play referendum (no tune
   line needed for the economy — it's default; add LEAN_YEAR=1 by hand).

## Run infrastructure (don't rediscover)

- Live app arm SIM_TUNE (tools pin these off): `DAWN_LIVE=1,STATE_RECORDS=1,LAND_KNOW=1,PEER_SEATS=1,FOUND_DRIFT=1,ABSORB_ORG_ERA=1,TRIBUTE_UP=0.33,ENGULF=8,FEAR_REACH=1,WAR_FINISH=1,SMALL_WAR=8,RELIEF_REACH=1,EXCH_WAVE=3,TECH_USE=1,VASSAL_LEVY=0.5,DISSOLVE_CORE=1,SETT_STRIDE=3,TRADE_STRIDE=5`
  (ONE_BOOK/SHIP_SURPLUS no longer needed — default since v45.)
- The day's instruments (all in tools/, headers carry their lessons):
  `probe_egyptfate` (box autopsy: series + event dump + life tables),
  `probe_anchormelt` (per-anchor death decomposition, dead-vs-survivor
  control, born@ line), `probe_zipf` (rank-size + the agglomeration fuel
  gauge), `probe_foodfunnel` (grain lane stage by stage), `probe_yieldcv`
  (the variance map vs literature bands), plus `probe_cellmass`,
  `probe_residualbite`.
- Gate battery pattern: 3 background tasks ≤2 runs each (`smoke`+`stylized
  8817` / `stylized 4242`+`777` / `resgate`), SIM_TUNE carries the lever;
  logs to docs/runs/. Beware `&&` chains: a budget-exceed exit skips the
  second run (bit us once).
- Measurement traps re-confirmed today (full list in the autopsy doc §5 +
  inline): live vs seeded-dawn regime split (777's fail is gate-regime; the
  win is live-regime — always name the regime), box composition (the Nile
  box median is DESERT — sample cropland), smoothed-rate folds converge to
  the MEAN (a ×10 inflated three logs), event-dump caps truncate tallies,
  realm-count-through-box has huge single-seed variance (70–99 across
  arms; don't read deltas <2× as treatment).

## Standing owner directives

Density is the ratified root problem ("no breathing room for realistic
cities. It is the exact source of all my issues") — LEAN_YEAR is its
answer, gated on the chain above. The harvest-years design is owner-
ratified ("actual year to year production swing across the globe, based on
real factors... wrapped into real output"). Commit and push every turn
(stop-hook). Three cardinal rules; measure before build; flip discipline —
all as in CLAUDE.md.

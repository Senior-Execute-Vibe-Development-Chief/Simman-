# The genesis lap — the Younger-Dryas anchor and the Neolithic clock (2026-08-26)

**SAVE_VERSION 49.** `T.CAGE_FILL` (Carneiro's pressure leg with the flight
knee) and `T.EPOCH_YD` (the display epoch re-anchored to t=0 = the end of the
Younger Dryas, ~9700 BCE), both flipped default-on after the full ladder.

## The owner's charge

> "I think one of our main problems is that farming is INVENTED at about
> 3000bc, and civilization appears shortly after that, which is the OPPOSITE
> of real life."
>
> "Civilization is closely linked to the end of the younger dryas, right? I
> think that should be our starting point if so." — ratified, "Go".

The real chain: Holocene onset (~9700 BCE) → farming within centuries-to-two-
millennia → **six thousand years of villages** → cities and writing ~3300-3200
BCE. The sim ran farming→writing in 1,688y — the Neolithic compressed ~3.4×,
and the display epoch (−5250, fitted to the old solver table) put farming on
screen at ~3200 BC. Both halves of the complaint were real.

## The measurement chain (every step in `docs/runs/2026-08-26/`)

**1. The scorecard** (`tools/probe_genesis.mjs`, obs-240 seed 8817): the tech
tree's own gates crossed by the world's leading knowledge, reported under both
epochs and against the real dates. Baseline (`genesis_base.log`): t0→farming
8,250 ticks (2,063y — top of the real 700-2,000y band); farming→writing 6,750
ticks (1,688y vs real ~5,800y); first city 14,000; ratio 0.82 vs real ~3-8.

**2. The conviction** (three ablation arms): `STATE_CAGE=0` kills genesis
entirely — no tallies, no writing, no city, no state by 22k. The caging drive
IS the whole pre-urban org engine, so its timing is genesis timing.
`ORG_CONTACT=0` and `EXCH_WAVE=0`: inert pre-urban. (`genesis_nocage.log`,
`genesis_nocontact.log`, `genesis_noexch.log`)

**3. The missing leg.** The cage field's own header names Carneiro's triad —
circumscription AND surplus AND **pressure** — but the drive was
`cage × storable` only: it fired full-strength at the first caged village,
millennia before the basin held anyone to pressure.

**4. `T.CAGE_FILL` v1 — linear fill** (`genesis_FILL.log`, 40k A/B): fill =
home-box people / home-box capacity (popField and capField, the two fields the
cage pass already reads; one extra box-sum in the same sweep). Writing 15,000
→ 17,250. Direction right, magnitude ~+2,250 of the ~+11,000 the anchor needs.

**5. The fill clock** (`tools/probe_fillclock.mjs`, `fillclock.log`): the
mechanism verified working — farming's capacity jump CRASHES fill (Nile 0.77
→ 0.05), the basin refills over ~8-11k ticks (an honest demographic pace;
`SETT_GROWTH` 0.0007/step ≈ 0.14%/y is the historical Neolithic band). The
leak was the SHAPE: linear fill lets a half-full basin run half-rate org the
whole way up (mean fill ≈ 0.45 → span stretched only 1.43×).

**6. The flight knee** (`cageField.js CAGE_FLIGHT_FREE`): Carneiro's argument
is a threshold, not a slope — while free land remains, a defeated village
flees and re-sows; no subjugation, no supra-village organization. Pressure =
`max(0, (fill − knee)/(1 − knee))`. The constant is bracketed by theory
([0.6, 0.8]: the village-fissioning land-stress point vs Carneiro's own
near-exhaustion reading, box-mean smoothing arguing below pointwise) and
placed within the bracket by the measured span. The ladder
(`genesis_KNEE.log`, `genesis_KNEE70.log`, `genesis_KNEE80.log`; first city /
writing, obs-240 seed 8817):

| arm | first city | writing | YD writing | ratio |
|---|---|---|---|---|
| baseline (no fill) | 14,000 | 15,000 | 5950 BC | 0.82 |
| linear fill | 16,500 | 17,250 | 5387 BC | 1.09 |
| knee 0.6 | 19,750 | 20,500 | 4575 BC | 1.48 |
| knee 0.7 | 21,000 | 21,750 | 4262 BC | 1.64 |
| **knee 0.8 (ships)** | **22,500** | **23,250** | **3887 BC** | **1.82** |

Real writing/cities: 3200 BC = tick 26,000 under −9700 at 0.25y/tick.

**The full chronology at knee 0.8, seed 8817, under the YD anchor:** farming
7637 BC (real 9000-7700) · pottery 7637 BC (real 6900 — granted with the
ledger's birth package, a named residual) · tallies 4075 BC (real 3700) ·
writing 3887 BC (real 3200) · bronze 4700 BC (real 3200; co-arrives with
writing as history had it) · sailing 4950 BC (real 5500) · galleys 2575 BC
(real 1500) · iron **1012 BC (real 1200)**. Baseline had cities at 6200 BC —
the two constants closed ~70% of the chronology error; the ~800y residual is
under single-seed noise. The era-skew lap's finding lands here too: the
post-Neolithic chain was already near-calibrated (iron within 190y); the
compression was specifically pre-urban.

**Dead theories, recorded per custom:** (a) the demographic rate — measured
honest, not the leak; (b) the ambient org floor at 0.5 — wrong arithmetic,
`ORG_CONTACT=8` puts the uncontacted-basin floor at 1/9 (history's
stateless-forever periphery is intact by default); (c) knee diminishing
returns as plateau convergence — partially: the 0.6→0.7 gain was onset delay,
the climb length holding ~7,300 ticks, because basins creep past both knees.

## The gate ladder (all green)

- **stylized 8817** @21k: ALL HARD, 0 warnings — and richer than baseline
  (31 alive, 23 polities, Zipf fittable at −0.74).
- **stylized 4242** @21k: ALL HARD, 1 soft (area tail 2.4) within budget.
- **stylized 777**: @21k the alive floor read 18 vs 20 with every shape gate
  green — the mechanism itself (fewer cities at fixed wall-clock IS the
  longer Neolithic). At the matched-age horizon 24k: ALL HARD, 24 alive,
  16 polities. **The battery's default horizon is re-baselined 21k → 24k
  with the flip** (stylized.mjs header carries the rationale: gates compare
  history's shape at matched urban age, not matched wall-clock — the same
  rule that bans time-gating mechanics).
- **resgate**: all app-grid bands held; median realm area app/ref **0.93**
  (near parity — the fill is a ratio of box-means, grid-robust by
  construction). `knee_resgate.log`
- **defaults smoke**: green pre-flip (lever-off byte-transparency) and
  re-run post-flip with the new defaults.
- Cross-seed genesis scorecards (777, 4242 at 36k): `genesis_KNEE80_777.log`,
  `genesis_KNEE80_4242.log` — the residual-systematicity record.

## The calendar re-anchor (`T.EPOCH_YD`, cosmetic both directions)

`DISP_START`/`DYN_START` (one fused clock, 0.25y/tick) move −5250 → −9700
under the lever. Audited before the change: `stepToYear`/`yearStr` have zero
callers (the −5600 mechanics clock is vestigial); `dynYear` feeds only rate
differences (loyaltyField) and absolute-year STAMPS in records (rulers'
`reignFrom`/`reignTo`, king-list `fromY`/`toY`) — never a branch. v<49 saves
pin `EPOCH_YD=0` (and `CAGE_FILL=0`) so an old world keeps its own pace and
a calendar its own stamped dynasty years agree with.

## Named residuals (future laps, all measured here)

- **Pristine vs secondary formation rate**: at drive 1 the pressure lane earns
  the contact lane's full rate — but subjugation-cycling built institutions
  ~3-5× slower than emulating a living example (packed-basin Ubaid→Uruk
  ~1,800-2,300y vs secondary Yamato/Ghana ~300-800y). If the ~800y residual
  proves systematic cross-seed, this is the grounded next constant.
- **The birth package's pottery**: `con 0.18` granted with the ledger, so
  pottery co-arrives with farming instead of ~2,100y later.
- **Village-lane navigation is 0**: sailing waits for courts, so it lands
  ~500-1,000y late (Ubaid boats were a village technology).
- The consolidation lap's parked items: `ABSORB_PEER` (owner's flip call),
  beyondDirectRule, the force lap (storm attacker-concentration).

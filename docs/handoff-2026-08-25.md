# Handoff — end of 2026-08-25 (the harvest-years day + the grain-market evening)

> **2026-08-26 UPDATE — SAVE_VERSION 48**: the provisioning lap flipped
> `T.GRAIN_PROVISION` (the annona — deficit + granary refill at the pace of
> the city's own mouths; breaks the measured self-sufficiency trap that
> pinned locally-fed cities at 12k with full treasuries). Referendum:
> peer-buyers 272/482, top3 share 0.16 (crowding-out refuted), Egypt box
> 0.24→0.58. Same lap shipped the Goods-flow overlay (Economy → "Goods":
> per-kind cargo streams, grain split levy vs market, toggle chips). Full
> record: the wave doc's provisioning section. The chain below stands —
> register-atomization/consolidation first (the statehood-arrives-last
> finding from the owner's t=36864 journal: integrate.orgBelowMin kills
> ~half of all integration candidates; the whole world is 6 states at 24k).

Read this first, then `docs/harvest-years-2026-08-25.md` (the wave's full
record: every measurement, formula, verdict and run-log reference — INCLUDING
the same-evening IMPORT/UPTAKE WAVE appended at its end: the open grain
market, `T.GRAIN_MARKET`, flipped ON as **SAVE_VERSION 47**). Branch
`claude/moisture-yield-validation-chain-o29gdc`, everything committed and
pushed. Tree state: SAVE_VERSION **47**; default hash baseline (320-grid
probe_hashbase, 2500 steps) is **31337 → ebfb8021 · 8817 → 7fb32527** —
UNCHANGED through the v47 flip (the market-institution gate makes it
byte-transparent until coinage exists); the pre-v46 baselines (c9562488,
ebd56121) are BOTH stale (the solver-tAmp refinement moved the default
world, then the v46 flip moved it again; the hash DEFINITION also changed
when _harvestYearMul joined SETT_HASH_NUM).

## THE EVENING'S WAVE (v47): cities finally BUY — the owner's 12k cure

The play-report's starving-12k half, executed end to end on the "go" order:
probe_uptake measured the funnel (the hungry register is LEAF-dominated —
82-84% of cities can neither buy nor SELL on the child→liege-only tree,
while the world grows 2.1× its cities' needs); `grainMarketPass` opened the
peer market (road+sea reach, cross-border, seller's scarcity price, spare
coin, no cross-border levy, zero new constants); the 777 gate seed forced
three rules into the shape (seed-corn: granaries refill before exports; the
market institution gate: buying needs coined money — dawn trade flattened
the early urban distribution and cost marginal worlds their first founding
cascade; the export capacity add-back: selling cannot drag a catchment
below its own land's yield — add-back, never a cross-tick max() floor); a
CHAOS ENSEMBLE (float-epsilon, no mechanism) exposed 777's recorded 40 as a
lucky draw over a 20-22 typical band hugging the hard floor at 20 — the
market cluster's tight 14-15 was real depression until the rules landed it
at 20 all-hard/0-warn. Flip battery: stylized 777/8817/4242 at 0/0/0
warnings, resgate all bands, smoke, hash pair unchanged. Referendum
(obs-240 30k): importers 3→175, fed(leaf) p50 0.08→0.58, W-Europe 0.85 /
Pontic 0.82 / India 0.70; the pre-gate obs-480 A/B read the same landslide
larger (importers 20→271, Egypt 0.84). NAMED RESIDUAL: Egypt/Mideast 0.24
in the final draw — atomized bronze singletons hold neither coin nor a levy
tree; its cure is realm consolidation or monetization (the
register-atomization wave), not a gate weakening. gm5_shape_1920.log is the
standing tw=960 spot-check arm.

## What SHIPPED today (defaults ON, v46, full ladder green)

The 2026-08-24 chain, executed end to end:

1. **The moisture-calibration lap.** The recorded "Britain ≈ Mesopotamia"
   debt dissolved without touching the index: the index is precipitation-like
   and its broken consumers were reading it as ARIDITY against flat
   thresholds. The one language is `em = moist / demand(temp)` —
   biomeClass's own calibrated Holdridge layer (England 0.72 vs Mesopotamia
   0.03 in the app regime). Solver tAmp gained its documented continentality
   refinement (westerly fetch + aridity, validated per-region vs NCEP
   amplitude; own full battery green). `probe_moistcal` is the instrument.
2. **The yield-variance map VALIDATES**: 11/12 literature regions in-band in
   the observed-climate regime at BOTH tw=240 and tw=480 (was 4/12). Formula
   lives in `src/sim/peopleSim/harvest.js` (shared code — probes score the
   real thing): em rain margin · seasonality (Gaussen ∨ amplitude-gated
   monsoon concentration) · winter risk on the COOL-HALF mean temperature ·
   flood regime (tFlood + channel band + "arid cropland is water-fed by
   construction"). The 12th region is Mesopotamia (0.35 vs ≤0.30): the
   in-grid Tigris-Euphrates never exceeds riverMag 2 and lays ~no floodplain
   — the pinned great-river resolution debt (riverGen/heightmap territory).
   Solver-regime arms read 4-5/12 with EVERY miss decomposing into the three
   named solver debts (em pattern too wet in Sahel/S.India/Iberia; dryFrac
   saturation — the fixed 0.55 dry-month bar; weak seasonal phase).
3. **T.HARVEST_YEARS** — the annual layer under climate.js's century walk:
   per harvest year each ~12° weather cell draws an AR(1) z (ρ=0.30),
   spatially smoothed; settlement landFood × (1 + z·cv) clamped [0.15,1.6];
   famine DERIVES from the tail (sub-p10 year destroying >35% — the retired
   FAMINE_SEVERITY's own loss bar) through the same `_famineUntil` consumers;
   the scripted spawner (aimed dice, flat severity, radius) retired under the
   lever. Mean multiplier 1.001 over 3000y (no hidden tax); famine ~13/1000y
   per region emergently (England ~2/millennium, Sahel chronic) vs the old
   dice's ~1.3/1000y planet-wide. `world._harvestZ` persists;
   `_harvestYearMul` is whitelisted AND hashed (the _thinBasinSince lesson);
   probe_harvestresume proves exact state round-trip.
4. **T.LEAN_YEAR re-grounded PER-BASIN** (crystallize.js `leanAt`): founding
   margin = the basin's own DEEP year, 1/(1 − 2.33·cv) clamped ≤5×
   (England ~1.4×, Nile flood ~1.9×, Mesopotamia ~2.3×, Sahel ~5×) at all
   seven bar sites, replacing the flat planetary 2.86×. Dissolve stays 1×.
5. **The flip** (both levers, one act, SAVE_VERSION 46, v<46 guard):
   stylized 8817/4242/777 all hard gates at 0/2/0 warnings — **seed 777
   released by physics** (the flat law had killed it: 10 settlements);
   smoke, resgate (all bands), monotone, coverage green; the Egypt
   live-regime referendum holds the flat law's anchor persistence exactly
   (big-anchor deaths 7 = 7, half the v45 control's 15) at the honest
   margin — standing register 24 (control 52, flat 14), turnover 132
   (control 375).
6. **Churn-era soft-band re-baseline** (the chain's authorized revisit):
   three n/a abstentions (Zipf-unfittable / <5 fallen polities / dead cradle
   origins) now score ok exactly when their record is demonstrably live
   (≥8 cities over the bar / ≥1 fallen / ≥1 root culture) and still warn at
   zero. Collapse-catches and all hard gates unchanged.

## Standing debts (named, measured, NOT this wave's)

- **The in-grid Tigris-Euphrates** (probe_floodmask: Fertile-Crescent window
  riverMag census 145/8/10/0/0/0 at tw=480): the twin rivers are sub-scale,
  so Mesopotamia farms as Gulf-delta alluvium and its CV reads high. The
  variance map's one red. riverGen/heightmap work, third-cardinal-rule
  territory.
- **Solver-regime climate pattern** (probe_moistcal columns): Sahel/S.India/
  Iberia too wet in em, temperate dryFrac saturated at the 0.55 bar, seasonal
  phase too weak for the monsoon-concentration signal. probe_climate_truth's
  territory; the app regime (observed climate) is unaffected.
- **The irrigation stack still reads raw moist** against IRRIG_ARID0=0.52
  (settlement.js `s._climMoist`, popField FIELD_CRADLE): Britain reads 35%
  "arid" to it, and a river-boosted tile SELF-DAMPS its own aridity read.
  The em language should extend there — its own lap with probe_irrfield as
  referee and the full ladder (capacity re-key world-wide).
- **famineVuln + FAMINE_CHANCE/RADIUS** stay in tuning for the legacy arm
  (v<46 saves); removable once the guard generation ages out.

## ADDENDUM — the play-report lap (same day, post-flip)

The owner's tw=960 play report (carpeting down; India/Caspian/Pontic great;
cities 12k-stuck and starving; Egypt/Mideast empty) was diagnosed and
partially fixed the same evening — full record in the wave doc's play-report
section; instruments probe_foundbar / probe_mintfunnel / probe_register.
The founding-margin surface at the app grid MATCHES the owner's healthy
list exactly; the Mideast was priced out (Mesopotamia p50 5×) by the
composite deep year's Gaussian tail on half-irrigated land. The fix —
per-component deep year + per-component annual draw on construct-water
land — ships as **T.ARID_SECURE (def 0)**: measured green on 8817 and on
the app-grid founding surface (Mesopotamia seats 1.9×, Levant 1.7×), still
RED on 777 (19 settlements paired; the credit needs the basin's watered
CAPACITY, not channel presence — the named refinement). Defaults are
verified byte-equivalent to the v46 green ladder across the full board.
A four-round elimination (mixfix1-6 logs) is recorded in the lever desc —
its standing lesson: the flip-validated cv formula is one organism; touching
any term moves every gate world.

The starving/12k half is measured, not yet built: cities run fed ≈ 0.27
with importShare 0.00 (the agglomeration target rides imports), Egypt
founds-then-bleeds inside a pinned peer capacity, and the observed-regime
world register explodes (157→528 over 10k steps at tw=480-obs). Those are
the import/uptake wave — the list below.

## THE CHAIN from here (updated after the v47 evening)

1. **The register-atomization / consolidation wave** — Egypt's residual
   (fed 0.24, no coin, no levy tree, singleton polities) and the
   observed-regime register explosion are now measured as ONE fact: the
   obs world's polities stay atomized (1.4-2.1 members/realm), so neither
   market channel reaches the bronze heartlands. Realm consolidation in the
   obs regime is the named cure — and the levy machinery is already built
   for it.
2. **Urban hierarchy / primacy** — the agglomeration engine now has margins
   (per-basin bars), surplus (v45), FLOWING IMPORTS (v47 — importShare p90
   1.00 at real cities) and annual scarcity signals (v46); primacy still
   ~1.0-1.1. URBAN_AGGLOM/URBAN_GAMMA, probe_zipf as referee.
3. **The owner's tw=960 play referendum** — the whole stack (economy +
   harvest years + per-basin founding + the grain market) is default; no
   tune line needed.
4. **CITY_HOLD / CATCH_GRACE re-trial** — right physics, measured red
   before density was fixed.
5. Buyer-side granary provisioning (the annona stockpile) if import-fed
   cities measure siege-fragile; the 777 alive-gate's chaos finding
   (typical 20-22 hugging the floor 20) for the next gate-grounding
   conversation; growing-season/moisture follow-ons (pkgSuitAt inputs).

## Run infrastructure (don't rediscover)

- The live app arm SIM_TUNE needs NO additions for today's work (all
  default): `DAWN_LIVE=1,STATE_RECORDS=1,LAND_KNOW=1,PEER_SEATS=1,FOUND_DRIFT=1,ABSORB_ORG_ERA=1,TRIBUTE_UP=0.33,ENGULF=8,FEAR_REACH=1,WAR_FINISH=1,SMALL_WAR=8,RELIEF_REACH=1,EXCH_WAVE=3,TECH_USE=1,VASSAL_LEVY=0.5,DISSOLVE_CORE=1,SETT_STRIDE=3,TRADE_STRIDE=5`
- Today's instruments (headers carry the lessons): `probe_moistcal` (climate
  axes vs NCEP truth, both regimes), `probe_yieldcv` v2 (--real arm,
  fert-weighted verdicts), `probe_floodmask`, `probe_nilebox`,
  `probe_harvest` (the mechanism referee), `probe_harvestresume`
  (paired-control resume check).
- Measurement traps confirmed again today: max-pool GHOSTS (the fert mask
  hangs valley fert on point-sample-desert tiles — weight verdicts by fert);
  box composition (Kyiv is not steppe, Guinea-savanna is not Sahel — draw
  boxes to the belts the literature bands describe); n/a-warnings punishing
  stability (distinguish abstention-with-live-record from broken-record);
  background gates on a mid-edit tree read as failures (re-run on the
  committed tree).

## Standing owner directives

Density ratified as the root problem — ANSWERED this wave (per-basin
founding + annual famine physics, default). The harvest-years design was
owner-ratified 2026-08-24 and is now default physics. Commit and push every
turn (stop-hook). Three cardinal rules; measure before build; flip
discipline — all as in CLAUDE.md.
